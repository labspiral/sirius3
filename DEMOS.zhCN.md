# 演示程序列表

## beginner
Sirius3 库初始化
各类设备（扫描仪、激光设备等）的初始化及与用户界面的连接

## editor_alc

这是一个利用自动激光控制（ALC）功能自动调节激光输出的演示程序。
- 基于定义向量（Defined Vector）的ALC功能，可在图层（Layer）笔中进行设置；通过加工开始和结束时的起始斜坡、结束斜坡比例值，可设置输出值呈线性变化。
- 基于速度（或加速度）依赖（Speed(or Velocity) Dependent）的ALC功能，可在图层（Layer）笔中进行设置；可选择最小值、最大值、输出信号及模式。
- 基于 SDC（点距控制）的 ALC 功能，可在图层（Layer）笔中进行启用设置，并通过实体（Entity）笔设置点距值    
- 此外，还包含通过测量功能对输出数据进行采样并以可视化方式显示结果的功能。

## editor_barcode

可生成多种形式的2D条码对象，并确定其单元格（Cell）组合。
此外，还可生成与条码具有相同数据的文本对象。
条码和文本对象的处理通过“实体（Entity）”笔进行设置。

## editor_barcode_textconvert

这是一个演示程序，展示了如何生成2D条码对象并修改其数据。
基于事件：用户可通过标记（Marker）的事件处理程序在处理前修改文本，并在处理时动态更新后应用至处理过程。
脚本：用户编写并指定C#脚本代码，处理时标记会利用脚本在处理前动态修改该条码的文本，并将其应用至处理过程。
外部文件：用户指定外部文件，在每次加工前读取文本文件的第一行，动态修改数据后应用于加工。此外，已使用的文本文件的第一行将被自动删除。
偏移量：用户指定包含多个位置（偏移量）信息的文本内容，加工时，标记器会在每个偏移量位置动态修改条形码的加工前文本内容，并将其应用于加工。

## editor_dio

这是一个利用RTC卡扩展端口数字输入/输出（Digital Input/Output）功能的演示程序。
用户可以为DIO的引脚编号指定名称，并通过输出显示系统状态（就绪、加工中、错误），
并实现了接收来自外部的加工开始信号（Start）以启动加工等功能。

## editor_entity

这是一个利用Sirius3生成的各种加工数据和矢量数据，并将其添加到文档（Document）和页面（Page）中的示例。
该示例展示了如何通过“块（Block）”和“插入块（Insert）”功能复用对象（Entity），同时也展示了如何通过“组（Group）”将多个对象（Entity）进行分组管理。	

## editor_fieldcorrection_2d

这是一个利用扫描仪二维场校正功能的演示程序。
在加工区域内以一定间隔标记图案，随后利用测量设备（例如：机器视觉）进行测量，将误差值传输至系统，从而基于当前的校正文件生成新的校正文件。
从而补偿二维空间中的非线性畸变。此外，还演示了如何将新生成校正文件加载（Load）到表格中并进行选择。

## editor_fieldcorrection_3d

这是一个利用扫描仪三维场校正功能的演示程序。
在两个Z空间位置分别以一定间隔标记图案，随后利用测量设备（例如：机器视觉）进行测量，将误差值传递给系统，从而基于当前校正文件生成新的校正文件。
在三维校准中，将计算Z空间（体积）的畸变线性比例（Stretch）值，并演示如何将新生成校准文件加载（Load）到表格中并进行选择。

## editor_fieldcorrection_3d_calibration

这是一个利用扫描仪三维场校正功能的演示程序。
通过输入3D点云（构成3D网格表面的点集合）来生成3D校正文件。
随后，若使用2D加工数据开始加工，激光焦点将自动移动至对应X、Y位置的3D网格表面，从而实现加工效果。
在应用程序层面上，只需提供X、Y数据，即可将其投影（Projection）至3D表面，从而实现3D表面上的激光加工。

## editor_hardjump

扫描仪的所有跳跃、标记（Jump、Mark）命令均以指定速度（mm/s）进行微向量处理，并以10微秒为周期执行。
本演示程序将跳跃区段设置为在单个周期（10微秒）内执行硬跳跃（Hard Jump），而非进行微向量处理。
虽然可以极大地缩短跳转所需的时间，但这可能会给扫描仪带来相当大的负担，因此应根据扫描仪的规格和加工数据的特性加以适当利用。

## editor_hatch

这是一个用于针对各种闭合曲线形状的对象生成填充（Hatching）图案的演示程序。
填充图案可以生成线（Line）和多边形（Polygon）形式，并可设置填充的间距、角度和起始位置等。
使用路径优化（Sort）功能，可优化填充路径，从而缩短加工时间；
还可以为每个填充区域设置不同的实体笔（Entity Pen），以调整加工条件。

## editor_hatch_clip

这是一个通过裁剪（Clipping）特定区域来生成填充（Hatching）图案的演示程序。

## editor_interrupt

RTC卡顾名思义是实时控制器（Real Time Controller）卡，它会预先将加工数据提供给列表缓冲区，
并进行实时处理，因此具有在加工过程中难以插入各种用户功能的特性。
本演示介绍了在列表缓冲区加工过程中发生中断（Interrupt），由用户应用程序处理后继续加工的中断功能。
该机制并非中断列表的执行，而是即使在处理过程中发生中断，列表缓冲区仍会保持执行待机状态。

## editor_laser_ui

在Sirius3中，只要继承并实现ILaser接口，即可扩展实现并使用任何激光设备。
此外，用户还可以直接设计并实现适合所实现激光设备的UI，并将其插入其中。
本示例演示了如何通过OnCreateLaserUI事件生成并插入用户设计的专用UI。

## editor_marker

Sirius3 内置了继承并实现了 IMarker 接口的标记器，具体包括 MarkerRtc、MarkerRtcFast 和 MarkerSyncAxis。
通过修改开源的标记器实现代码并将其直接连接到 Sirius3，用户可以按所需方式自定义加工方式。

## editor_measurement_skywriting_wobbel

这是一个通过图层笔（Layer Pen）选择多种天空书写（Skywriting）模式（1、2、3、4）并将其应用于加工的演示。
此外，这也是一个通过实体笔（Entity Pen）利用各种抖动（Wobbel）形状的演示程序。
此外，还包含通过测量功能对输出数据进行采样并以可视化方式显示结果的功能。

## editor_mof_interrupt

MoF（Marking on the fly）功能是将外部移动物体产生的编码器信号输入到RTC卡中，使扫描仪实时追踪并进行加工的功能。
本演示是利用editor_interrupt的中断功能与MoF功能相结合的复合型演示。

开始MoF加工时，在加工该物体之前会触发中断，此时将工作台（或传送带等）移动至中心位置。
随后，系统将等待编码器到达该位置，此时启动MoF（扫描仪开始追踪），并在加工完成后终止MoF。

## editor_mof_offsets

MoF（Marking on the fly）功能是将外部移动物体产生的编码器信号输入到RTC卡中，使扫描仪能够实时追踪并进行加工的功能。
为了在非常大的大面积区域内对单个加工形状进行MoF加工，使用虚拟图像场（Virtual image field）。
该方式是在等待编码器值达到1000个偏移（Offset）位置的数值，一旦满足条件便立即进行加工。
此处假设偏移量的X坐标位于-200至0的空间范围内，且工作台（或传送带）向右侧移动，
即扫描仪的X值增加的方向。
此外，通过“实体笔”（Entity Pen），不仅展示了速度、功率等加工条件，还演示了如何使用RTC6与excelliSCAN组合的SCANAhead选项功能。

## editor_mof_trigger

MoF（Marking on the fly）功能是将外部移动物体产生的编码器信号输入到RTC卡中，使扫描仪实时追踪并进行加工的功能。
此处是一个示例，通过RTC的扩展D.IN输入端口等待外部触发信号，一旦接收到触发信号，立即开始MoF加工。
此外，在每次条码加工开始前，用户可生成所需的条码数据并预先插入RTC缓冲区，从而实现动态数据加工。
本文还演示了如何利用自由变量（Free Variable），在触发信号输入后，每当实际条码对象加工完成时累积其次数。

## editor_mof_xy

MoF（飞行标记）功能是指将外部移动物体产生的编码器信号输入到RTC卡中，使扫描仪能够实时跟踪并进行加工的功能。
本演示展示了如何创建编码器等待条件，并针对每个单独的物体分别进行设置。

## editor_multibeam

这是一个利用多光束（Multibeam）功能的演示程序，该功能通过将一个激光源拆分为2个扫描头 + 2个AOM的组合进行加工。
当一侧扫描头在加工过程中开始跳转（Jump）时，激光输出将关闭，
此时另一侧扫描头的待机状态解除并开始加工，遇到跳转（Jump）区间时再将控制权交还，以此循环的控制方式。
这是一种通过最大限度提高激光光源利用率来提升生产效率的方式。

## editor_multiple

这是一个演示程序，通过分别创建2个系统（扫描仪、激光装置等）+ 2个编辑器及数据（文档），利用多头系统对不同的数据进行加工。

## editor_multiple2

这是一个演示程序，通过创建2个系统（扫描仪、激光装置等）+ 1个编辑器及数据（文档），利用多头系统对相同的数据进行加工。

## editor_offset

这是一个将加工数据在多个位置（偏移）上进行重复加工的演示程序。
偏移量可分别设置不同的 dx、dy、dz 移动距离、z 轴旋转角度以及缩放比例（scale）等参数。

## editor_pen

这是一个演示如何通过图层笔（Layer Pen）和实体笔（Entity Pen）更改加工条件的演示程序。
此外，它还展示了如何覆盖图层笔和实体笔所应用的功能（通过标记事件）。

## editor_pen_multiple

本节演示了如何通过覆盖设置来设定图层笔（Layer Pen）和实体笔（Entity Pen）的初始值。
此外，还演示了如何为不同对象设置不同的实体笔（Entity Pen），从而改变加工条件。

## editor_points_sync_pulses_count

在点（或圆点）加工时，通常采用激光在指定时间内保持激活（LASER ON）的加工方式。
这是一个演示程序，展示如何将激光源输出的 SYNC OUT 信号输入到 RTC 卡中作为同步信号，
并利用 SYNC OUT 信号的脉冲数来控制激光针对该点（或圆点）的激活（LASER ON）时间。
此外，该演示程序还展示了其他可用同步信号的使用方法。

## editor_powermap

本演示程序展示了如何创建补偿映射（或称补偿表）以弥补激光源输出与用户期望的实际输出之间的差异，并演示了其使用方法。
此外，my_powermap.cs 是关于实际映射、验证和补偿流程的开源代码，用户可通过修改该代码，按自身需求灵活运用补偿映射。

## editor_scanahead

这是一个利用 RTC6 支持的 SCANAhead 功能的演示程序。
启用“自动延迟”（Auto delays）功能后，扫描器跳转、多边形、标记延迟时间以及激光开关延迟时间将自动计算并应用。
此外，实体笔（Entity Pen）的属性列表会根据“自动延迟”功能自动显示或隐藏。

## editor_scanahead_sdc

这是一个利用 RTC6 支持的 SCANAhead 功能的演示程序。
它展示了如何通过图层笔 (Layer Pen) 组合并启用 SCANAhead 功能与 ALC 功能，
以及如何通过实体笔 (Entity Pen) 设置 SDC（点距控制）距离值。

## editor_slicer

这是一个演示程序，用于加载3D网格模型，将其沿指定的Z平面进行切片（Slicing）以提取轮廓（Contours），
并在该区域插入填充图案等增材制造（AM）功能。

## editor_steppermotor

这是一个利用RTC卡提供的步进电机（Stepper Motor）扩展端口进行控制功能的演示程序。


## editor_syncaxis

这是一个用于操作 XL-SCAN 系统（而非普通 RTC 卡）的演示程序。
专为 ACS 运动控制器 + excelliSCAN（或 intelliSCAN iV）+ RTC6 组合的大面积扫描系统设计，
通过 syncAXIS 库展示如何实现扫描仪与运动控制器的同步控制以及运动分解功能。

## editor_ui

这是 Sirius3 库内部提供的 SiriusEditorControl、SiriusMultiEditorControl 用户控件的开源代码。
用户可利用此开源代码设计并定制所需的 UI。
