# 데모 프로그램 목록


## beginner

시리우스3 라이브러리 초기화
각종 장치 (스캐너, 레이저 장치 등) 초기화 및 UI 연결

## console_document

콘솔 환경에서 각종 장치 (스캐너, 레이저 장치 등) 초기화 및 문서(Document) 를 이용한 마커(Marker) 예제

## console_syncaxis_setup

RTC6 기반 XL-SCAN 시스템을 syncAXIS로 직접 설정하고 점검하는 콘솔 데모입니다.
`syncAXISConfig.xml`을 불러와 시뮬레이션/하드웨어 모드와 Follow/Unfollow 동작을 전환하고, 스캐너와 스테이지 이동, 사각형·원 가공, 스캐너 보정, 레이저 지연 및 시스템 지연 확인 작업을 실행합니다.

## editor_automatic_laser_control

Automatic Laser Control (ALC) 기능을 사용하여 레이저 출력을 자동으로 조절하는 데모 프로그램입니다. 
- Defined Vector 기반의 ALC 기능으로 활성화는 레이어(Layer) 펜에서 설정할 수 있으며, 가공 시작과 끝에서 시작 램프, 종료 램프 비율값을 통해 출력값이 선형적으로 변화하도록 설정합니다.
- Speed(or Velocity) Dependent 기반의 ALC 기능으로 활성화는 레이어(Layer) 펜에서 설정할 수 있으며, 최소, 최대값및 출력 신호, 모드를 선택할 수 있습니다.
- SDC(Spot Distance Control) 기반의 ALC 기능으로 활성화는 레이어(Layer) 펜에서 설정할 수 있으며, 개체(Entity) 펜을 통해 Spot 거리값을 설정할	
- 또한 계측기능을 통해 출력 데이타를 샘플링하고 그 결과를 시각적으로 표시하는 기능도 포함되어 있습니다.

## editor_barcode

다양한 형태의 2D 바코드 개체를 생성하고 그 셀(Cell) 조합을 결정할 수 있습니다.
또한 바코드와 동일한 데이타를 갖는 텍스트 개체를 생성합니다.
바코드와 텍스트 개체의 가공은 개체(Entity) 펜을 통해 설정합니다.

## editor_barcode_textconvert

2D 바코드 개체를 생성하고 그 데이타를 변경하는 방식을 보여주는 데모 프로그램입니다.
- 이벤트 기반 : 사용자가 마커(Marker) 의 이벤트 핸들러를 통해 가공 직전 텍스트를 변경하고 이를 가공시 동적으로 변경후 가공에 적용합니다.
- 스크립트 : 사용자가 C# 스크립트 코드를 작성해 지정하고 가공시 마커는 가공직전 해당 바코드의 텍스트를 스크립트를 이용해 동적으로 변경후 가공에 적용합니다.
- 외부 파일 : 사용자가 외부 파일을 지정하고 데이타를 매 가공 직전 텍스트 파일에서 맨 첫줄을 읽어 데이타를 동적으로 변경후 가공에 적용합니다. 또한 사용한  텍스트파일의 첫줄은 자동 제거 됩니다.
- 오프셋 : 사용자가 여러 위치(오프셋) 정보와 함께 텍스트 내용을 지정하고, 가공시 마커는 매 오프셋 위치에서 바코드를 가공 직전 텍스트 내용을 동적으로 변경후 가공에 적용합니다.

## editor_dio

RTC 카드의 확장포트의 디지털 입출력(Digital Input/Output) 기능을 활용하는 데모 프로그램입니다.
DIO 의 핀 번호에 사용자가 이름을 지정할 수 있으며, 시스템의 상태(준비, 가공중, 에러)를 출력으로 나타내고
외부에서 가공 시작과 같은 신호(Start)를 입력 받아 가공을 시작하는등을 구현하고 있습니다.

## editor_document

가공 데이타(즉 Recipe) 는 Document 라는 객체에서 관리되며, 다양한 개체(Entity)들을 생성해 이 Document 에 저장하게 됩니다.
이때 Editor 는 지정된 Document 를 렌더링 하게 됩니다. 외부에서 여러개의 Document 를 만들어 교체하는 방법과 함께 뷰어(Viewer)를 통해 하나의 Document 를
다양한 에디터, 뷰어에 출력(렌더링) 하는 예를 보여줍니다.

## editor_entity

시리우스3에서 제공되는 다양한 가공 데이타 및 벡터 데이타들을 생성해 문서(Document) 및 페이지(Page)에 추가하는 예제입니다.
블럭(Block)과 블럭 삽입(Insert) 을 통해 개체(Entity)를 재사용하는 방법을 보여주고, 그룹을 통해 여러 개체(Entity)를 묶어 관리하는 방법도 보여줍니다.	

## editor_entity_custom

응용 프로그램에서 필요한 사용자 정의 엔티티를 Sirius3에 추가하는 방법을 보여주는 데모입니다.
마름모, 피듀셜, 드릴 홀 엔티티를 직접 구현해 속성 편집, 형상 재생성, 렌더링, 복제, 해칭, 가공 및 문서 추가 과정을 보여줍니다.

## editor_fieldcorrection_2d

스캐너 2차원 필드 보정 기능을 활용하는 데모 프로그램입니다.
가공에 사용되는 영역에 일정 간격으로 패턴을 마킹한후 이를 계측기(예: 머신비전)를 이용해 측정하 오차값을 전달해 현재의 보정파일에서 새로운 보정파일을 생성합니다.
2차원 공간의 비선형적인 왜곡을 보상하게 됩니다. 또한 신규로 생성된 보정파일을 테이블에 로드(Load) 하고 이를 선택하는 방법을 보여줍니다.

## editor_fieldcorrection_3d

스캐너 3차원 필드 보정 기능을 활용하는 데모 프로그램입니다.
RtcCalibrationLibrary 를 사용해 스캐너 3차원 필드 보정 기능을 활용하여 절차적인 보정법을 보여주고 있습니다.
- Z 움직임에 따라 빔의 기울어지는(Tilt) 보정
- Z = 0 에서 최적의 포커스 위치를 찾는 보정
- Z공간에서 다양한 위치의 최적의 포커스를 찾아 Zout = A + Bl + Cl² 방정식의 계수(A,B,C)를 찾는 보정
- Z 상하 공간에서 Stretch 크기 오류를 보상하는 보정

## editor_fieldcorrection_3d_pointscloud

스캐너 3차원 필드 보정 기능을 활용하는 데모 프로그램입니다.
3D 포인트 클라우드(3D 메쉬의 표면을 구성하는 점 집합) 를 전달해 Z 높이값을 갖는 3D 보정 파일을 생성합니다.
이후 2D 가공 데이타를 이용해 가공을 시작하면, 해당 X,Y 위치 들은 3D 메쉬 표면으로 레이저의 초점이 자동으로 이동되는 효과를 얻게 됩니다.
응용프로그램 레벨에서는 X,Y 데이타만 전달하면 이를 3D 표면으로 투영(Projection)되는 효과를 얻게 되어, 3D 표면 위에 레이저 가공이 가능하게 됩니다.

## editor_hardjump

스캐너의 모든 점프, 마크(Jump , Mark) 명령은 지정된 속도(mm/s)를 마이크로 벡터 처리되어, 10usec 주기로 명령이 처리됩니다.
이 데모에서는 점프 구간을 마이크로 벡터 처리대신 한 사이클(10usec)에 하드 점프(Hard Jump)로 설정하는 데모 프로그램입니다.
점프에 소요되는 시간이 극단적으로 줄어들는 효과를 얻을수 있으나, 스캐너에 상당한 부담이 될 수 있으므로, 스캐너의 사양과 가공 데이타의 특성에 따라 적절히 활용해야 합니다.

## editor_hatch

다양한 폐곡선 형태의 개체에 대해 해칭(Hatching) 패턴을 생성하는 데모 프로그램입니다.
해치는 선(Line) 과 폴리곤(Polygon) 형태로 생성할 수 있으며, 해치의 간격과 각도, 시작 위치 등을 설정할 수 있습니다.
경로 최적화(Sort)를 사용하면, 해치의 경로가 최적화되어 가공 시간이 단축되는 효과를 얻을 수 있으며,
각 해치별로 서로 다른 개체펜(Entity Pen) 을 설정해 가공 조건을 달리 할 수 있습니다.

## editor_hatch_clip

특정 영역을 클리핑(Clipping)하여 해칭(Hatching) 패턴을 생성하는 데모 프로그램입니다.

## editor_interrupt

RTC 카드는 말그대로 Real Time Controller 카드로, 리스트 버퍼에 미리 가공 데이타를 제공하고
이를 실시간으로 처리 하기 때문에 가공 도중에 다양한 사용자 기능을 삽입해 사용하기 어려운 특성이 있습니다.
이 데모에서는 리스트 버퍼 가공 중간에 인터럽트(Interrupt) 가 발생하고 이를 사용자 응용프로램측에서 처리 후 가공을 이어가는 인터럽트 기능을 소개합니다.
리스트 실행이 멈추는 방식이 아닌 중간에 인터럽트가 발생해도 리스트 버퍼는 계속 실행대기 상태가 되는 방식입니다.

## editor_laser_ui

시리우스3에서는 ILaser 인터페이스 를 상속구현하면 어떠한 레이저 장치라도 확장 구현해 사용이 가능합니다.
또한 사용자가 구현한 레이저 장치에 적합한 UI 역시 직접 디자인 및 구현해 이를 삽입할 수 있습니다.
이 예제에서는 OnCreateLaserUI 이벤트를 통해 사용자가 디자인한 전용 UI 를 생성해 삽입하는 방법을 보여줍니다.

## editor_marker

시리우스3 라이브러리 내부에서 제공되는 MarkerRtc, MarkerRtcFast, MarkerSyncAxis 의 오픈소스 코드 입니다.
이 오픈소스를 수정해 시리우스3 에 직접 연결시키면, 사용자가 원하는 방식으로 가공방식을 커스터마이징 할 수 있습니다.

## editor_measurement_skywriting_wobbel

레이어 펜(Layer Pen)을 통해 다양한 스카이 라이팅(Skywriting) 1,2,3,4 모드를 선택하고 이를 가공에 적용한 데모입니다.
또한 개체 펜(Entity Pen)을 통해 다양한 와블(Wobbel) 형상을 활용하는 데모 프로그램입니다.
또한 계측기능을 통해 출력 데이타를 샘플링하고 그 결과를 시각적으로 표시하는 기능도 포함되어 있습니다.

## editor_mof_interrupt

MoF(Marking on the fly) 기능은 외부 이동물체에서 발생하는 엔코더 신호를 RTC 카드에 입력시켜 스캐너가 실시간 이를 추종하면서 가공하는 기능입니다.
이 데모에서는 editor_interrupt 의 인터럽트 기능과 MoF 기능을 활용한 복합적인 데모입니다.
MoF 가공을 시작하면, 해당 개체 가공 직전 인터럽트가 발생하고 이때 스테이지(혹은 컨베이어 등) 를 중심위치로 이동시킵니다.
이후 엔코더가 해당 위치에 도달할때까지 대기후 MoF 가 시작(스캐너 추종 시작) 및 가공을 완료후 MoF 종료 하는 예제입니다.

## editor_mof_offsets

MoF(Marking on the fly) 기능은 외부 이동물체에서 발생하는 엔코더 신호를 RTC 카드에 입력시켜 스캐너가 실시간 이를 추종하면서 가공하는 기능입니다.
하나의 가공 형상을 매우 큰 대면적 위치에 MoF 가공하기위해 가상 이미지 필드 (Virtual image field) 사용합니다.
- 1000개의 오프셋(Offset) 위치에 해당하는 엔코더값이 될때까지 대기하면서 조건에 맞으면 즉시 가공하는 방식입니다.
- 여기에서는 오프셋의 X 좌표가 -200 ~ 0 공간에 있다고 가정하고, 스테이지(혹은 컨베이어)가 우측 방향으로, 
- 즉 스캐너의 X 값이 증가하는 방향으로 이동한다고 가정합니다.
- 개체 펜(Entity Pen) 을 통해 속도, 파워 등 가공 조건뿐 아니라 RTC6 + excelliSCAN 조합의 사용한 SCANAhead 옵션 기능을 사용하는 방법을 보여줍니다.

## editor_mof_trigger

MoF(Marking on the fly) 기능은 외부 이동물체에서 발생하는 엔코더 신호를 RTC 카드에 입력시켜 스캐너가 실시간 이를 추종하면서 가공하는 기능입니다.
- 외부 트리거를 RTC의 확장 D.IN 입력 포트로 대기하고, 트리거가 입력되면 즉시 MoF 가공을 시작하는 예제입니다. 
- 매 바코드 가공 직전 사용자가 원하는 바코드 데이타를 생성해 RTC 버퍼에 미리 삽입시켜 동적 데이타를 가공할 수 있습니다.
- 자유변수(Free Variable) 를 활용해 트리거 입력 후 실제 바코드 개체가 가공이 완료될 때마다 그 회수 누적시키는 방법도 보여줍니다.

## editor_mof_xy

MoF(Marking on the fly) 기능은 외부 이동물체에서 발생하는 엔코더 신호를 RTC 카드에 입력시켜 스캐너가 실시간 이를 추종하면서 가공하는 기능입니다.
엔코더 대기 조건을 만들고 이를 개별 개체별로 각각 설정하는 방법을 보여줍니다.

## editor_mof_xy_raster

MoF(Marking on the fly) 기능은 외부 이동물체에서 발생하는 엔코더 신호를 RTC 카드에 입력시켜 스캐너가 실시간 이를 추종하면서 가공하는 기능입니다.
- 여기에서는 Image, 1D, 2D 바코드, ImageText 와 같이 레스터(Raster)가공이 가능한 경우 매 줄(Line) 가공 직전 엔코더 위치를 대기합니다.
- 대기에서 탈출되면 스캐너(혹은 렌즈)의 중심 위치에서 레스터 가공이 되도록 하는 기능을 제공합니다. 
- 원통 형상을 눕히고 회전시키면서 원통의 옆면에 로고 등을 MoF + 레스터 가공하는데 효과적입니다.

## editor_multibeam

하나의 레이저 소스를 2개의 스캔헤드 + 2개의 AOM 조합으로 분할해 가공하는 멀티빔(Multibeam) 기능을 활용한 데모 프로그램입니다.
- 한쪽 스캔헤드가 가공중 점프(Jump) 시작시 레이저 출력이 꺼지게 되는데, 
- 이때 반대편 스캔헤드가 대기가 풀리면서 가공을 처리하고, 점프(Jump) 구간을 만나면 다시 제어권한을 반납하기를 반복하는 제어 방식입니다.
- 전체적으로 레이저 소스의 사용율을 극대화해 생산성을 높히는 방식입니다.

## editor_multibeam2

하나의 레이저 소스를 공유하는 한 쌍의 RTC MultiBeam 인스턴스를 운용하는 UI 데모입니다.
헤드 간 신호 배선을 확인하고 Head 1, Head 2, Both 모드와 준비 헤드를 선택하며, 각 헤드 또는 두 헤드의 Ready, Start, Stop, Reset 동작을 제어합니다.

## editor_multiple

2개의 시스템(스캐너, 레이저 장치 등) + 2개의 편집기 및 데이타(Document) 을 각각 생성해 서로 다른 데이타를 멀티 헤드 시스템을 사용해 가공하는 데모 프로그램입니다.

## editor_multiple2

2개의 시스템(스캐너, 레이저 장치 등) + 1개의 편집기 및 데이타(Document) 을 생성해 동일한 데이타를 멀티 헤드 시스템을 사용해 가공하는 데모 프로그램입니다.

## editor_offset

가공 데이타를 여러 위치들(오프셋)에 반복 가공하는 데모 프로그램입니다.
오프셋은 dx, dy, dz 이동거리, z 축 회전 각도, 비율(scale)등을 서로 다르게 적용 가능합니다.

## editor_pen

레이어 펜(Layer Pen), 개체 펜(Entity Pen) 을 통해 가공 조건을 변경하는 방법을 보여주는 데모 프로그램입니다.
또한 레이어 펜 및 개체 펜 이 적용되는 기능(마커의 이벤트를 통햬)을 오버라이드 하는 방법도 보여줍니다.

## editor_pen_multiple

레이어 펜(Layer Pen), 개체 펜(Entity Pen) 이 최초 생성될때 초기값을 설정하기 위해 오버라이드 하는 방법을 보여줍니다.
또한 다양한 개체들에 서로 다른 개체 펜(Entity Pen) 을 설정해 가공 조건을 달리 하는 방법도 보여줍니다.

## editor_points_sync_pulses_count

점(도트 혹은 픽셀) 가공시에는 통상 지정된 시간동안 레이저가 활성화(LASER ON) 되는 가공 방식을 사용합니다.
- 레이저 소스에서 출력되는 SYNC OUT 신호를 RTC 카드에 입력시켜 동기 신호로 활용하고,
- 해당 점(혹은 도트)에 대해 레이저가 활성화(LASER ON) 되는 시간을 SYNC OUT 신호의 입력 펄스로 출력 펄스를 동기화 합니다.
- 그 밖에 사용가능한 다양한 동기 신호들에 대해 사용법을 보여주는 데모 프로그램입니다.

## editor_powermap

레이저 소스의 출력이 사용자가 원하는 실제 출력과의 차이를 보상하는 보상 맵(혹은 테이블) 을 만들고 이를 사용하는 방법을 보여줍니다.
또한 my_powermap.cs 는 실제 이 매핑, 검증, 보상 하는 절차에 대한 오픈 소스 코드로 이를 수정해 사용자가 원하는 방식으로 보상 맵을 활용할 수 있습니다.

## editor_remote

원격 통신 (시리얼, TCP/IP, 웹소켓, MQTT)으로 연결을 시도하고, 외부 시스템을 통해 마커, 오프셋, 개체 값 읽고 쓰기 등의 조작을 통신을 통해 처리하는 데모 프로그램입니다.
여기에서는 웹소켓을 활용한 예제를 보여주고 있습니다.

## editor_scanahead

RTC6에서 지원되는 SCANAhead 기능을 활용하는 데모 프로그램입니다.
Auto delays 기능을 활성화 하게 되면 스캐너 점프, 폴리곤, 마크 지연 시간 및 레이저 온, 오프 지연시간이 자동으로 계산되어 적용됩니다.
또한 개체 펜(Entity Pen) 의 속성 목록들이 Auto delays 기능에 맞도록 자동으로 보이거나 감추어집니다.

## editor_scanahead_sdc

RTC6에서 지원되는 SCANAhead 기능을 활용하는 데모 프로그램입니다.
레이어 펜(Layer Pen)을 통해 SCANAhead 기능 및 ALC 기능을 조합하고 이를 활성화 하는 방법과,
개체 펜(Entity Pen)을 통해 SDC(Spot Distance Control) 거리값을 설정하는 방법을 보여줍니다.

## editor_script

마커 스크립트로 가공 직전에 엔티티 데이터를 바꾸는 방법을 보여주는 데모입니다.
`TextConverters.SimpleScript`를 사용하는 SiriusText를 만들고 스크립트 식으로 일련번호를 생성하며, `.script`, C# 소스 또는 DLL 파일의 스크립트 인스턴스를 확인, 저장, 초기화, 로드 및 컴파일하는 방법을 제공합니다.

## editor_slicer

3D 메쉬 모델을 불러와 지정된 Z 평면으로 슬라이싱(Slicing) 해 그 경로(Contours)를 추출하고,
이 영역에 해치를 삽입하는 등의 AM(Additive Manufacturing) 기능의 데모 프로그램입니다.

## editor_steppermotor

RTC 카드에서 제공되는  스테퍼 모터(Stepper Motor) 확장포트를 사용한 제어 기능을 활용하는 데모 프로그램입니다.

## editor_stitchedimage

카메라 격자, 이미지 해상도와 시야각 정보를 이용해 `EntityStitchedImage`를 만드는 데모입니다.
스캐너를 각 타일 위치로 이동하고 파일에서 카메라 이미지를 취득하는 과정을 모사해 `View.StitchedImage`에 합성 결과를 표시하며, 이미지를 지우고 다시 구성하는 방법도 보여줍니다.

## editor_syncaxis

일반적인 RTC 카드가 아닌 XL-SCAN 시스템을 사용하기 위한 데모 프로그램입니다.
ACS 모션 컨트롤로 + excelliSCAN(or intelliSCAN iV) + RTC6 조합의 대면적 스캐닝 시스템 전용으로,
syncAXIS 라이브러리를 활용해 스캐너와 모션 컨트롤러의 동기 제어및 Motion Decomposition  기능을 사용하는 방법을 보여줍니다.

## editor_ui

시리우스3 라이브러리 내부에서 제공되는 SiriusEditorControl, SiriusMultiEditorControl 사용자 컨트롤의 오픈소스 코드 입니다.
이 오픈 소스코드를 활용해 사용자가 원하는 UI를 디자인하고 커스터마이징이 가능합니다.

## editor_viewer

가공 데이타(즉 Recipe) 는 Document 라는 객체에서 관리되며, 다양한 개체(Entity)들을 생성해 이 Document 에 저장하게 됩니다.
이 Document 를 에디터 및 뷰어와 연결하여 출력(렌더링) 하는 예를 보여줍니다. 즉 One Document, Multiple View 예제를 제공합니다.

## editor_zpl

오프라인 BinaryKits 렌더러를 이용해 여러 ZPL 라벨 예제로 `EntityImageZPL`을 만드는 데모입니다.
라벨 크기와 인쇄 해상도 설정, 유니코드 ZPL 데이터, `^CW` 폰트 매핑 및 로컬 미리보기와 레이저 가공에 사용할 대체 폰트 설정 방법을 보여줍니다.
