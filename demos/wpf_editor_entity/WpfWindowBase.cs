using System.Windows;

namespace Demos
{
    /// <summary>
    /// Supplies the WPF base type expected by the shared <c>Form1</c> entity samples.
    /// The WinForms project resolves the same unqualified name to its framework Form.
    /// </summary>
    public class Form : Window
    {
    }
}
